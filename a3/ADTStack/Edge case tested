| Edge Case         | Example                         | Expected Result                                     |
| ----------------- | ------------------------------- | --------------------------------------------------- |
| Empty expression  | `""`                            | Program should handle it safely                     |
| Unknown variable  | `x y +` where `y` is not stored | Program should show variable not found              |
| Division by zero  | `5 0 /`                         | Program should throw or show division by zero error |
| Malformed token   | `5 2 @`                         | Program should reject invalid token                 |
| Missing operands  | `5 +`                           | Program should handle stack error                   |
| Normal expression | `2 3 +`                         | Result should be `5`                                |


Also copy this as **`QA_TESTING.md`**:

```markdown
# QA Testing Report

## Role

Part 6 — QA, Packaging, and Presentation Lead

## Purpose

The purpose of QA testing was to make sure the full postfix calculator project worked correctly before submission. The project uses an array-based stack for postfix evaluation and a Binary Search Tree to store variables.

## Testing Checklist

### 1. Normal Expression Testing

Tested regular postfix expressions to confirm the calculator gives the correct result.

Example:

```text
Expression: 2 3 +
Expected Result: 5
